import { WorkoutTimePipe } from './workout-time.pipe';

describe('WorkoutTimePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkoutTimePipe();
    expect(pipe).toBeTruthy();
  });
});
